name: CI/CD Workflow

on:
  push:
    branches: ['*']
    tags: ['*']
  pull_request:
    branches: ['*']

permissions:
  contents: write
  checks: write
  pull-requests: write

jobs:
  windows:
    name: Build Windows
    uses: ./windows-build.yml

  linux:
    name: Build Linux
    uses: ./linux-build.yml
